import {number, string} from 'prop-types'

const product = {
    title: string,
    description: string,
    quantity: number,
    priceTotal: number,
    pricePerItem: number,
}

export default {
    product,
}
